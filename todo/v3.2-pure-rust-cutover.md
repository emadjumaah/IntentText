# v3.2 Pure Rust Cutover Tracker

Target release: `3.2.0`
Created: `10/03/2026`
Status: `in-progress`

## Scope Lock

Release rule:

- `3.2.0` removes TS runtime parser/renderer paths from shipped core.

Change policy until Step 4 exit:

- No non-parity feature merges.
- Only parity, regression, packaging, CI, and migration work.

## Owners

- Core (Rust/WASM/runtime routing): Emad
- CLI (Rust binary + JS launcher compatibility): Emad
- Python bridge/thin-layer: TBD
- Docs/changelog/release messaging: TBD
- CI/parity/size gates: Emad

## Milestone Schedule

- M0 Scope freeze + owners assigned: 10/03/2026
- M1 Fallback telemetry + blocker triage: 10/03/2026
- M2 Theme parity in Rust: 10/03/2026
- M3 TS runtime parser/renderer removed: 10/03/2026
- M4 npm + Python thin layers complete: in-progress
- M5 Rust CLI default + trust commands parity: in-progress
- M6 UK date-format contract rollout: TBD

## Step 1 Checklist (Freeze Scope + Rules)

- [ ] Assign all owners above.
- [ ] Publish cutover announcement to all package maintainers.
- [x] Add changelog note draft: `3.2.0` removes TS runtime parser/renderer.
- [ ] Confirm non-parity feature freeze is active.
- [ ] Confirm tracker is source of truth for cutover status.

Step 1 exit criteria:

- [ ] Tracker has owner names and deadlines.
- [ ] Scope freeze communicated and acknowledged.

## Step 2 Prep (Telemetry)

Fallback counters required:

- [x] Parser fallback to TS.
- [x] Renderer fallback to TS.
- [x] Theme fallback path.
- [x] WASM load/call failure fallback.

Reporting:

- [x] Expose counters in CI artifact output (`npm run telemetry:report` in `packages/core`).
- [ ] Weekly parity summary includes fallback counts.

## Dependency Map

Core packages impacted:

- `IntentText`
- `intenttext-rust`
- `intenttext-python`
- `intenttext-mcp`
- `intenttext-vscode`
- `intenttext-editor`
- `intenttext-desktop`

## Decision Log

- `10/03/2026`: Tracker created locally in repo as cutover source-of-truth scaffold.
- `10/03/2026`: Editor pagination work paused; cutover plan resumed.
- `10/03/2026`: Added runtime fallback telemetry counters in `packages/core/src/rust-core.ts` for parser option fallback, renderer option fallback, renderer theme fallback, wasm load fallback, and wasm call fallback.
- `10/03/2026`: Added telemetry unit tests in `packages/core/tests/rust-core-telemetry.test.ts`.
- `10/03/2026`: Added CI-friendly telemetry JSON report command `packages/core: npm run telemetry:report`.
- `10/03/2026`: Wired `IntentText/.github/workflows/ci.yml` to emit `core-fallback-telemetry.json` and upload it as a workflow artifact.

## Step 3 Prep (Theme Parity)

Contract doc:

- `IntentText/todo/theme-parity-contract-v3.2.md`

Initial status:

- [x] Resolution order documented.
- [x] CSS output invariants defined.
- [x] Snapshot parity gate and failure policy defined.
- [x] Rust-path theme resolution implemented in core runtime wrapper.
- [x] Step 3 unit tests added for metadata/option precedence and fallback behavior.

## Step 3 Exit Gate Progress

- [x] Parity suite green for all builtin themes.
- [x] `renderer_theme_fallback_to_ts` remains zero in CI.

Current notes:

- Theme fallback branch has been removed from Rust render path in `rust-core`.
- Rust path now applies theme precedence: `options.theme > metadata.meta.theme > corporate`.
- Builtin-theme matrix tests now run across representative fixture types in `packages/core/tests/rust-core-theme-parity.test.ts`.
- Remaining work: verify CI artifact trend keeps `renderer_theme_fallback_to_ts` at zero.

## Decision Log (Step 3)

- `10/03/2026`: Removed renderer metadata/option TS fallback in `packages/core/src/rust-core.ts` by adding Rust-path theme CSS injection with precedence `options.theme > metadata.meta.theme > corporate`.
- `10/03/2026`: Added Step 3 tests in `packages/core/tests/rust-core-theme-parity.test.ts`.
- `10/03/2026`: Expanded Step 3 tests to builtin-theme matrix coverage across representative fixtures (simple, callout-heavy, code/quote/link, print-layout).
- `10/03/2026`: Added CLI runtime telemetry command `node cli.js telemetry [--json] [--reset]` to surface Rust-core fallback counters during migration.
- `10/03/2026`: Added CI hard-fail guard in `.github/workflows/ci.yml` to enforce `renderer_theme_fallback_to_ts == 0` using telemetry JSON output.
- `10/03/2026`: Added release note draft file `IntentText/todo/v3.2.0-release-note-draft.md` for Step 1 messaging requirement.

## Step 4 Progress (TS Runtime Parser/Renderer Cut)

Status:

- `in-progress` (transition mode)

Completed in this pass:

- [x] Removed public engine-selection routing flags/env switches from `packages/core/src/rust-core.ts`.
- [x] Rust parse path now applies `includeHistorySection` directly without parser-option TS fallback.
- [x] Regression suite validated after routing changes (`867` tests passed in `packages/core`).

Remaining for Step 4 exit:

- [x] Remove emergency TS renderer fallback path from `renderHTML` once remaining Rust renderer parity gaps are closed.
- [x] Confirm parser/renderer runtime no longer executes TS implementation paths under supported production feature set.
- [x] Create and maintain explicit blocker list for Rust renderer parity burn-down: `IntentText/todo/step4-renderer-parity-blockers.md`.

Step 4 blocker source-of-truth:

- `IntentText/todo/step4-renderer-parity-blockers.md` (`0` open renderer items after v2.11 burn-down on `10/03/2026`).

## Decision Log (Step 4)

- `10/03/2026`: Removed external engine toggle routing (`__INTENTTEXT_CORE_ENGINE` / `INTENTTEXT_CORE_ENGINE`) from core runtime selection logic.
- `10/03/2026`: Added Rust-path `includeHistorySection` handling in `rust-core` using history boundary extraction + parsed history attachment.
- `10/03/2026`: Kept internal emergency TS renderer fallback for wasm render failures after parity validation showed unresolved Rust renderer coverage for several v2.11/docgen render scenarios.
- `10/03/2026`: Completed Step 4 item-2 first batch (`byline`, `epigraph`, `caption`, `dedication`, `footnote` + docgen integrations) and reduced open renderer parity blockers from `25` to `15`.
- `10/03/2026`: Added wasm payload normalization in `packages/core/src/rust-core.ts` (property/meta/context value coercion + diagnostics omission) to prevent serde mismatch fallbacks.
- `10/03/2026`: Cleared remaining v2.11 renderer parity blockers (`ref`, `def`, `figure`, `signline`, `contact`, `deadline`) and removed TS renderer emergency fallback from `packages/core/src/rust-core.ts`.
- `10/03/2026`: Removed parser-side TS runtime fallback paths from `packages/core/src/rust-core.ts` (including `options.extensions` fallback and wasm-failure fallback) and validated full core suite (`870/870`).
- `11/03/2026`: Removed semantic-validator TS runtime fallback from `rust-core` and patched Rust-path parity for `HISTORY_WITHOUT_FREEZE` (history present with no freeze) without reintroducing TS validate runtime routing.

## Step 5 Progress (Thin npm Layer)

Status:

- `completed`

Completed in this pass:

- [x] Added package-size baseline reporter `packages/core/scripts/report-package-size.cjs`.
- [x] Added npm script `packages/core: npm run size:report`.
- [x] Wired CI to emit and upload `core-package-size.json` artifact.
- [x] Removed `dist` sourcemap outputs from published build (`sourceMap=false`, `declarationMap=false`, clean build before emit).
- [x] Added CI size gate `packages/core/scripts/check-package-size.cjs`.
- [x] Added parser-runtime-coupling guard `packages/core/scripts/check-no-parser-runtime-coupling.cjs`.

Baseline (`11/03/2026`, mapless clean build):

- `dist.fileCount`: `61`
- `dist.mapFileCount`: `0`
- `dist.totalBytes`: `918,849`
- `wasm.bytes`: `581,659`
- `npm pack --dry-run`: packed `262,512`, unpacked `925,524`, entries `63`

Step 5 incremental status (`11/03/2026`):

- [x] Switched core runtime parse call paths (`merge`, `trust`, `history`, browser entry) from `parser.ts` to `rust-core`.
- [x] Moved `detectHistoryBoundary` export from `parser.ts` to `trust.ts` for entrypoint decoupling.
- [x] Verified built entry files no longer require `./parser` at runtime.
- [x] Replaced shipped `dist/parser.js` implementation with a thin compatibility shim that forwards to `rust-core` and `trust` exports.

Active Step 5 gate thresholds:

- `dist.totalBytes <= 950,000`
- `wasm.bytes <= 600,000`
- `dist.mapFileCount == 0`
- `npm pack packedBytes <= 265,000`
- `npm pack unpackedBytes <= 940,000`
- `npm pack entryCount <= 63`

## Decision Log (Step 5)

- `10/03/2026`: Introduced baseline package-size telemetry for `@intenttext/core` (`dist` total bytes/files + wasm size) and published as CI artifact for Step 5 reduction tracking.
- `10/03/2026`: Enforced clean mapless build output for `@intenttext/core` and added CI size gate script with baseline thresholds.
- `10/03/2026`: Added compiler comment stripping (`removeComments=true`) and tightened CI `dist.totalBytes` threshold to `1,000,000` after new baseline.
- `10/03/2026`: Added CI npm publish-footprint gate (`check-pack-footprint.cjs`) to enforce packed/unpacked/install-file-count limits from `npm pack --dry-run`.
- `11/03/2026`: Rerouted core parse runtime imports in `merge.ts`, `trust.ts`, `history.ts`, and `browser.ts` to `rust-core`; built entry files no longer load `./parser`.
- `11/03/2026`: Added `detectHistoryBoundary` to `trust.ts` and moved package export to avoid parser runtime coupling in `index`.
- `11/03/2026`: Added CI/runtime guard script to fail if built core entrypoints (`index/browser/merge/trust/history`) require `./parser`.
- `11/03/2026`: Added build-time parser runtime shim (`scripts/shim-parser-runtime.cjs`) so published `dist/parser.js` is a thin compatibility bridge over `rust-core`; reduced package footprint baseline.

## Step 7 Progress (Rust CLI Default)

Status:

- `in-progress`

Completed in this pass:

- [x] Added Rust binary target `intenttext-cli` in `intenttext-rust/Cargo.toml`.
- [x] Implemented initial CLI scaffold in `intenttext-rust/src/main.rs` (parse/render/conversion subset).
- [x] Added Rust CLI usage examples in `intenttext-rust/README.md`.
- [x] Added Rust CLI `validate` subcommand (text/JSON output).
- [x] Added Rust CLI `query` subcommand (text/JSON output).
- [x] Added Rust CLI `index` subcommand (`--recursive` support).
- [x] Added Rust CLI `ask` subcommand (`--format text|json`).
- [x] Added Rust CLI trust subcommands: `seal`, `verify`, `history`, `amend`.
- [x] Reduced JS CLI to Rust-first launcher handoff with JS compatibility fallback (`IntentText/cli.js`).

Remaining for Step 7 exit:

- [x] Command parity for `parse`, `render`, `query`, `validate`, `index`, `ask`.
- [x] Trust parity for `seal`, `verify`, `history`, `amend`.
- [x] JS launcher reduced to wrapper/default handoff path.

## Step 6 Progress (Thin Python Layer)

Status:

- `in-progress`

Completed in this pass:

- [x] Added optional Rust semantic-validation bridge in `intenttext-python` via Node core (`INTENTTEXT_PY_VALIDATE_ENGINE=rust`).
- [x] Added compatibility code mapping for Python validation issue codes (`GATE_APPROVER_REQUIRED`, `DUPLICATE_ID`, `VARIABLE_UNRESOLVED`) on Rust-path outputs.
- [x] Preserved Python-specific `POLICY_GATE_REQUIRED` behavior in Rust validation mode for compatibility.
- [x] Added optional Rust-backed query bridge in `intenttext-python` (`INTENTTEXT_PY_QUERY_ENGINE=rust`) with automatic fallback and regex-filter compatibility guard.
- [x] Added optional Rust-backed HTML render bridge in `intenttext-python` (`INTENTTEXT_PY_RENDER_ENGINE=rust`) with fallback to Python renderer.
- [x] Preserved `include_css=False` compatibility in Rust render mode by stripping injected style tags at bridge boundary.
- [x] Added optional Rust-backed source conversion bridge in `intenttext-python` (`INTENTTEXT_PY_SOURCE_ENGINE=rust`) with fallback to Python source conversion.
- [x] Verified focused source tests pass (`3` tests) and adjacent parser/query/renderer/validate subset stays green (`115` tests).
- [x] Added optional Rust-backed trust-helper bridges in `intenttext-python` (`INTENTTEXT_PY_TRUST_ENGINE=rust`) for history-boundary detection, hash, seal, and verify.
- [x] Added Python-compatibility result mapping for Rust trust outputs (`frozen_at`, `expected_hash`, `signed_current_version`).
- [x] Flipped semantic validation default to Rust (`INTENTTEXT_PY_VALIDATE_ENGINE=rust` default) while preserving Python fallback and compatibility code mapping.
- [x] Added Rust-validation compatibility guard for Python `STEP_REF_MISSING` semantics (`depends`, `decision then/else`, `parallel steps`) after default flip.

Remaining for Step 6 exit:

- [ ] Move remaining runtime helpers (`render`, `source`, trust helpers) behind Rust-first bridge paths with compatibility-normalized outputs.
- [x] Move remaining runtime helpers (`trust helpers`) behind Rust-first bridge paths with compatibility-normalized outputs.
- [x] Decide default for `INTENTTEXT_PY_VALIDATE_ENGINE` after parity confidence window.
